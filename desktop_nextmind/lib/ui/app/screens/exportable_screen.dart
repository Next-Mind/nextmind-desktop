import 'package:flutter/material.dart';

class ExportableScreen extends StatelessWidget {
  const ExportableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}